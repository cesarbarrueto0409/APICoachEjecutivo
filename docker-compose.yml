services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bedrock-api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - bedrock-network

networks:
  bedrock-network:
    driver: bridge
